name: Build from Confluence

on:
  workflow_dispatch: # Allows manual execution only

jobs:
  call-publish-confluence:
    uses: originlab/originlab.github.io/.github/workflows/publish_confluence.yml@main
    with:
      root_page_id: "42860862"
